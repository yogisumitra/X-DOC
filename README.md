# X-DOC-LOCK — prototipe

Dua layar prototipe yang berjalan sepenuhnya di browser, tanpa server.

- `index.html` — portal on-boarding (x-doc-lock.id)
- `tenant.html` — aplikasi tenant (x-doc.lock.<nama-klien>)

Publikasi: GitHub Pages, Settings -> Pages -> Deploy from a branch -> main -> / (root).
